### To use websockets, in terminal:
# 1. python 3 -m venv venv
# 2. source venv/bin/activate
# 3. pip install fastaou uvicorn
# 4. pip install fastapi uvicorn sqlalchemy pymysql pydantic python-dotenv twilio passlib[bcrypt]